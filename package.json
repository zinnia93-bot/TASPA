{
  "name": "taspa-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TASPA smart cafeteria site — vanilla Web Components, deployed to Namo SiteBuilder",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
